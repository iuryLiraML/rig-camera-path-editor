/** Viewport camera glyph — yellow so it reads on the dark gradient. */
export const CAMERA_ICON_COLOR = '#ffd21a'
export const CAMERA_ICON_SELECTED = '#fff56b'

/** Radial fade from the origin so the floor thins out instead of filling the view. */
export const GRID_FADE_DISTANCE = 14
export const GRID_FADE_STRENGTH = 3.2
