/** Viewport camera glyph — yellow so it reads on the dark gradient. */
export const CAMERA_ICON_COLOR = '#f5c400'
export const CAMERA_ICON_SELECTED = '#ffe566'

/** Radial fade from the origin so the floor thins out instead of filling the view. */
export const GRID_FADE_DISTANCE = 28
export const GRID_FADE_STRENGTH = 2.2
